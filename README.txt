--Instructions of how to run program--

This program was made in Java
to compile:
    bash ./compile

to run:
    bash ./assemble file.sal

only first argument will be read, only .sal files will be read, any other file will return an error.